# Shared contracts

Versioned Zod schemas shared by the website and Guide Runtime. Browser clients
never send arbitrary executable content: progress and simulator snapshots cross
the service boundary as validated JSON values.
